function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  textAlign(CENTER, CENTER);
  textFont('TimesNewRoman');
  textSize(24)
  text("Aqui estará el examen",width/2,height/2);
}